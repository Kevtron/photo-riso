Place one or more images here (PNG, JPEG, WebP, TIFF, BMP, GIF). Everything in examples/ is gitignored except this README.txt.

scripts/batch_all_mask_combos.py scans this folder by default (--input-dir) and runs the full ink sweep for each file. Outputs: batch_outputs/<sanitized_filename_stem>/ per source image.
